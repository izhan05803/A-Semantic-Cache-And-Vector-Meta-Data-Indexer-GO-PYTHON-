apiVersion: v1
kind: Service
metadata:
  name: indexer
  namespace: semantic-cache
spec:
  selector:
    app: indexer
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
    - name: metrics
      port: 8000
      targetPort: 8000
